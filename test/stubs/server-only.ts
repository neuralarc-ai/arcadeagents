// Test stub for the `server-only` package. In production this import makes the Next
// bundler error if a server-only module is pulled into client code. In unit tests we
// run the modules directly in node, so it is a harmless no-op.
export {};
